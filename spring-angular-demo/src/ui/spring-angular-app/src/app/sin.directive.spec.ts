import { SinDirective } from './sin.directive';

describe('SinDirective', () => {
  it('should create an instance', () => {
    const directive = new SinDirective();
    expect(directive).toBeTruthy();
  });
});
